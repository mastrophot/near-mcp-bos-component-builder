# Example BOS Scaffolds

This directory contains three pre-generated outputs from `bos_scaffold_component`:

- `demo-minimal`
- `demo-token-swap`
- `demo-portfolio`

They are included as reference deliverables for template quality and deployment script format.
